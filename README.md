# PSYCare România

Platformă digitală pentru psihologi și pacienți.

## Setup

Terminal 1 (Server):
```bash
cd server
npm install
npm run dev
```

Terminal 2 (Client):
```bash
cd client
npm install
npm run dev
```

Frontend: `http://localhost:3000`  
Backend: `http://localhost:3001`
